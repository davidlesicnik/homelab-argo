apiVersion: v1
kind: Service
metadata:
  name: lemur-pitwall
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: lemur-pitwall
  ports:
    - name: web
      port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP

